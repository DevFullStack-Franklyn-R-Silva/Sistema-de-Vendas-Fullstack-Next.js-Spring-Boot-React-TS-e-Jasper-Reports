import { Layout } from "components/layout";

export const CadastroProdutos: React.FC = () => { 
    return (
        <Layout titulo="Produtos">
            Aqui vai ficar o meu formulario
        </Layout>
        
    )
}